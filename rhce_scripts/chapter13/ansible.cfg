[defaults]
remote_user = ansible
host_key_checking = false
inventory = listings/my_inventory_list
nocows=1
stdout_callback = debug

[privilege_escalation]
become = True
become_method = sudo
become_user = root
become_ask_pass = False

